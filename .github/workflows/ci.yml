name: CI

on:
  pull_request:
    branches: [main]

  workflow_dispatch:

jobs:
  config:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Lint
        run: echo Check lint

      - name: JSON
        run: echo Check json

      - name: Single update
        run: echo Check one update at a time

  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Code freeze
        run: echo Check if code freeze

      - name: Live release
        run: echo Check if release is live
